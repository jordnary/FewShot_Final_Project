# FroFA Results

云端运行后，本目录保存 LibFewShot 的 `config.yaml`、`checkpoints/`、`log_files/`，以及 `frofa_vs_baseline_summary.md/.csv`。

当前本机 RTX4060 和内存不用于正式中大规模测试；正式数值请在云端运行 `05_paper_reproduction/run_frofa_cub_cloud.sh` 后生成。
